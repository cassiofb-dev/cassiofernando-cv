name: Build and Upload CVs

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Build and compile CVs using Docker Compose
        run: |
          docker compose up

      - name: Rename and prepare artifacts
        run: |
          mv cv/en/cassiofernando-cv.pdf cassiofernando-cv.en.pdf
          mv cv/pt-br/cassiofernando-cv.pdf cassiofernando-cv.pt-br.pdf

      - name: Upload English CV
        uses: actions/upload-artifact@v7
        with:
          name: cassiofernando-cv.en.pdf
          path: cassiofernando-cv.en.pdf

      - name: Upload Portuguese CV
        uses: actions/upload-artifact@v7
        with:
          name: cassiofernando-cv.pt-br.pdf
          path: cassiofernando-cv.pt-br.pdf
